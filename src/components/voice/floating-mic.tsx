"use client";

/**
 * @deprecated Separate voice FAB removed. Grok voice lives in ChatWidget
 * (launcher mic + composer). This component is a no-op for any stale imports.
 */
export function FloatingMic() {
  return null;
}
